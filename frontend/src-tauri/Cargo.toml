[package]
name = "study-hub-timer"
version = "0.1.0"
description = "Study Hub floating timer"
authors = ["Study Hub"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
